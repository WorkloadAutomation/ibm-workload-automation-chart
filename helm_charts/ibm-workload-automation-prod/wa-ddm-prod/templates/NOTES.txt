Your IBM Workload Automation Dynamic Domain Manager is starting.

{{- if not (eq (.Values.ddm.exposeServiceType | upper ) "NONE") }}
{{- if .Values.ddm.routes.enabled }}
{{- if .Values.ddm.routes.hostname }}
To verify if the DDM is successfully installed use the following URL: https://{{ .Values.ddm.routes.hostname }}/twsd
{{- else }}
To verify if the DDM is successfully installed use the following URL: https://{{ .Release.Name }}-waddm.mycluster.proxy/twsd
{{- end }}
{{- else }}
To verify if the DDM is successfully installed use the following URL https://{{ template "waDdm.ServiceName" . }}:31116/twsd from a browser inside the cluster.
To test it from outside the cluster you can use:
  kubectl port-forward statefulset/{{ include "wa.fullName" . }} <local_port>:31116
and navigate to: https://localhost:<local_port>/twsd
{{- end }}
{{- end }}